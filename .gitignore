# dependencies
node_modules/

# npm
npm-debug.log*
*.tgz

# macOS
.DS_Store

# editor
.idea/
.vscode/
*.swp

# temp
*.log

# runtime per-profile A2A config (also written to cwd when a profile starts
# outside its own directory — never commit it)
a2a.json
# internal agent debug docs (never published)
.agent/

# screenshot capture temp metadata
*.rect.json
